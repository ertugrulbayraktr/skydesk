# Copy to .env and fill in. Required by docker-compose.
# JWT signing key — must be at least 32 characters.
JWT_SECRET=replace-with-a-long-random-string-min-32-chars
# Optional: enables real Gemini AI (otherwise the app runs in mock mode).
GEMINI_API_KEY=
